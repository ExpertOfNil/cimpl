# CIMPL

A library implemented in C with a focus on being simple to understand and use.

This library is implemented as header-only, but static and shared archives are
created when compiled with CMake:
```shell
cd cimpl
cmake -B build
cmake --build build
```

# Notes

- Currently, this library is only compatible with Linux
- Windows compatibility is on the roadmap
